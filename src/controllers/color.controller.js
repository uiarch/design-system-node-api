import Abstract from './abstract.controller';

/**
 * @export
 * @class ColorController
 * @extends {Abstract}
 */
export default class ColorController extends Abstract {
  /**
   * Creates an instance of ColorController.
   *
   * @param {*} model
   * @memberof ColorController
   */
  constructor(model) {
    super(model);
  }
}
